__pycache__/
*.pyc
.DS_Store
push/build/
push/dist/
push/*.spec
push/ChainPush.app/
push/*.log

# the workflows live in .github/workflows/; these are the copies the bridge
# could drop at the root, and a place for anything that is only yours
/github-workflow-*.yml
local/

# a folder written to the wrong place; delete it when you see it
_to_delete/

# The commit message waiting to be used. Written before the commit by whoever
# made the change, used by commit.bat, and deleted the moment it has been -
# so it is never committed and never goes stale.
/commit-msg.txt
